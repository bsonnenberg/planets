#!/usr/bin/python

#This script generates a list of numbers 
#Then iterates through the list adding one at each iteration

#creates list of integers ranging from 1 to 11
num_list = list(range(1,11))

#uses for loop to iterate through each item in list
for num in num_list:
#prints each number (plus 1) until finished
	print(num+1)



